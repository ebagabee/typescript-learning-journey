// Basic types
let firstname: string = "João";
let age: number = 25;
let active: boolean = true;

// type inference
let lastname = "Silva";
let number = 42;

// Arrays
let numbers: number[] = [1, 2, 3];
let names: Array<string> = ["Ana", "Pedro"];

// Tuple
let coordinates: [number, number] = [10, 20];

// Enum
enum StatusUser {
  Active = "ACTIVE",
  Inative = "INACTIVE",
  Pending = "PENDING",
}
